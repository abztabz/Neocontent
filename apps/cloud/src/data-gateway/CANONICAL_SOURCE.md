// Canonical shared source governance lives in abztabz/NeoOS-Source-Registry.
//
// This directory is temporarily vendored for NeoContent production compatibility.
// Do not introduce new provider-policy decisions here first. Shared capability,
// provider lifecycle, licensing, privacy, priority and gateway-contract changes
// must originate in the standalone registry and then be synchronized/consumed.
//
// NeoContent-specific research interpretation remains outside this directory.
export const canonicalNeoOsSourceRegistry = "abztabz/NeoOS-Source-Registry" as const;
